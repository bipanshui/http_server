# Custom HTTP Server and Router

A lightweight Http Server built using Nodejs http module in Typescript

It supports 📦: 
- Routing ('GET', 'POST', 'PUT', 'PATCH', 'DELETE')
- Route parameters('/users/:id')
- Query parameters('?key=value')
- Middleware system
- JSON body parsing
- Custom response helpers ('res.json', 'res.status', 'res.send')
- Logger middleware

I made this project to learn how Express works internally and for first principle thinking

## 🛠️ Tech Stack
- Nodejs
- Typescript
- native 'http' module

 ## 🛣️ Available Routes
- GET  /
- GET  /users/:id
- POST /users
- GET  /status

👨‍💻 Author

Bipanshu Kr.

Learning backend & system design.
